# Cursor Landing — dual-host (Q14 keep_both). Cursor Agent, search, and @ ignore these paths.
# Use only when grill Q14 answer is keep_both — skip this file on cursor_only (see MERGE-TO-RULES.md § Grill Q14).
# Antigravity / Gemini CLI do not read .cursorignore — AGENTS.md and GEMINI.md stay for those tools.
# Merge: append lines only; do not remove user entries if .cursorignore already exists.

AGENTS.md
GEMINI.md
**/GEMINI.md
.agent/

# Optional — enable when grill Q14 keep_both + sub-ask yes (ignore CLAUDE.md for Cursor):
# CLAUDE.md
