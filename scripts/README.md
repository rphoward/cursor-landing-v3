# Scripts (reserved)

v1 scan is **checklist-driven** via Explore and [references/scan-checklist.md](../references/scan-checklist.md)—no bundled executable.

Add read-only scanner scripts here only if dogfood shows agents repeatedly miss the same signals. Any script must infer shell from the repo, emit markdown, and fall back to the checklist on failure.
